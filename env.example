# NIWA Tide API Configuration
# Get your API key from: https://developer.niwa.co.nz/
# Copy this file to .env and add your actual API key
NIWA_TIDE_API_KEY=your_niwa_tide_api_key_here

# Google Earth Engine Service Account Email
# This should match the service account email in your .private-key.json file
GEE_SERVICE_ACCOUNT=your-service-account@your-project.iam.gserviceaccount.com

# Optional: Google Earth Engine Project ID
# GEE_PROJECT_ID=your-project-id

# Test Mode Configuration (for testing with limited data)
# Set TEST_MODE=true to enable test mode with limited data download
TEST_MODE=false
TEST_START_DATE=2023-01-01
TEST_END_DATE=2024-12-31
# Note: TEST_SITES should match the site type being processed:
#   - For batch_process_NZ.py: use NZ sites (e.g., nzd0001, nzd0002, nzd0003)
#   - For batch_process_sar.py: use SAR sites (e.g., sar0001)
#   - For full workflow: set to match sites you want to test (e.g., nzd0001,sar0001)
TEST_SITES=nzd0001
TEST_SATELLITES=L8,L9

# Validation Configuration (for validating against original data)
# Set FORCE_START_DATE to force a specific start date (overrides incremental processing)
# This is useful when validating against original data to ensure exact date range matching
# FORCE_START_DATE=1999-08-17

